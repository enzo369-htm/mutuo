import { WorkPageContent } from "@/components/WorkPageContent";

export const metadata = {
  title: "Projects · Mutuo Agencia",
};

export default function WorkPage() {
  return <WorkPageContent />;
}
